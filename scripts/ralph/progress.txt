## Codebase Patterns
- Rust modules use `pub mod` in lib.rs when types need to be accessible from Tauri commands in the same file
- All Rust types that cross the Tauri IPC boundary use `#[serde(rename_all = "camelCase")]` for frontend interop
- Cargo workspace dependencies (serde, serde_json, dirs) are referenced via `dep.workspace = true` — no version needed
- `cargo check -p mrunner` is the quality gate for Rust changes (not `cargo build`)
- tsup bypasses `noEmit: true` in tsconfig.json (uses esbuild internally) — safe to use even with type-only tsconfig
- tokio `macros` feature cannot be used (tokio-macros 2.6.0 not in local registry); use `tokio::spawn` for concurrency instead of `tokio::join!`
- tokio async process I/O: take stdout/stderr handles from child BEFORE entering timeout so child stays owned for kill(); spawn tasks for concurrent reads
- CommandAction union is in `apps/renderer/src/core/types/command.ts`; new action types must be added there AND in the switch in `use-commands.ts` to avoid `never` error
- `core/types/index.ts` and `commands/types.ts` are re-export barrels — new types/guards must be added to both
- Plugin action dispatch: use `executePluginAction` from `apps/renderer/src/lib/execute-plugin-action.ts`; navigation callbacks (push) passed as options to keep utility stateless
- Settings tabs wired in `settings-sheet.tsx`: TABS const + label render + Alt+N range + SheetBody block + i18n key; `UserPreferences` interface and `UserPreferencesSchema` must stay in sync

# Ralph Progress Log
Started: Mon Mar  9 12:36:44 PM -03 2026
---

## 2026-03-09 - US-001
- Created `apps/launcher/src/plugins.rs` with PluginTier, CommandMode, RegisteredCommand, RegisteredPlugin types
- Added `pub mod plugins;` to `apps/launcher/src/lib.rs`
- All types derive Debug, Clone, Serialize, Deserialize with serde rename_all = camelCase
- Files changed: apps/launcher/src/plugins.rs, apps/launcher/src/lib.rs
- **Learnings for future iterations:**
  - The branch `feat/plugin-system` did not exist on master — had to create it; prd.json/progress.txt existed only on the feature branch (stash conflict on creation)
  - `pub mod plugins` needed (not just `mod plugins`) so sibling Tauri command functions in lib.rs can access the types
  - cargo check passes in ~3s for this crate
---

## 2026-03-09 - US-002
- Implemented `discover_plugins()` function in `apps/launcher/src/plugins.rs`
- Added private helper structs `PluginManifest` and `CommandConfig` for JSON deserialization
- `discover_plugins()` scans `~/.config/mrunner/plugins/` using `dirs::config_dir()`
- Tier 2 detection: directories containing `plugin.json` are treated as scriptable plugins
- For each Tier 2 plugin: parses `plugin.json` manifest, then scans `src/commands/*/config.json`
- Command IDs formatted as `{plugin_id}:{command_dir_name}`; script path is `{cmd_dir}/command.ts`
- Invalid/missing files are logged to stderr and skipped gracefully (no crashes)
- Files changed: apps/launcher/src/plugins.rs
- **Learnings for future iterations:**
  - `dirs::config_dir()` is already in workspace deps — no changes to Cargo.toml needed
  - `entries.flatten()` is idiomatic for skipping `DirEntry` errors silently
  - `std::fs::read_dir` + `serde_json::from_str` is sufficient; no extra deps needed
---

## 2026-03-09 - US-003
- Added `#[tauri::command] async fn discover_plugins()` in `apps/launcher/src/lib.rs`
- Function calls `plugins::discover_plugins()` and returns `Vec<plugins::RegisteredPlugin>`
- Registered the command in the `invoke_handler` array in `lib.rs`
- Files changed: apps/launcher/src/lib.rs
- **Learnings for future iterations:**
  - Tauri commands wrapping existing module functions are minimal — just a thin async wrapper calling the sync function
  - `async fn` wrapper is idiomatic for Tauri commands even when the underlying function is sync
  - Register new commands in the `tauri::generate_handler![]` macro at the bottom of `lib.rs`
---

## 2026-03-09 - US-004
- Created `packages/plugin/` directory as a new workspace package
- `package.json`: name `@mrunner/plugin`, private: false, no runtime deps, TS dev dep only
- `tsconfig.json`: matches packages/ui pattern (no base extend, noEmit: true, strict mode)
- `src/index.ts`: exports CommandContext, Accessory, ListItem, Action (union type), ListResult, DetailResult, ActionResult, CommandResult, and abstract Command class
- `Command` class has abstract `run()` and optional `onItemSelect?()` method
- pnpm-workspace.yaml already covers `packages/*` — no changes needed
- pnpm install succeeded and package recognized in workspace
- Files changed: packages/plugin/package.json, packages/plugin/tsconfig.json, packages/plugin/src/index.ts, pnpm-lock.yaml
- **Learnings for future iterations:**
  - `packages/*` glob in pnpm-workspace.yaml already covers new packages — no edits needed
  - `private: false` is required in package.json for a publishable SDK package
  - Optional methods in abstract classes: `onItemSelect?(...)` syntax (no `async` keyword in declaration)
  - biome auto-fixed one file (likely trailing whitespace/formatting in the new src/index.ts)
---

## 2026-03-09 - US-005
- Created `packages/plugin/src/runner.ts` as a CLI entrypoint
- Runner reads CommandContext JSON from stdin, dynamically imports command module from argv[2]
- Uses `tsx/esm/api` register() to support importing TypeScript command files at runtime
- Validates exported default is a function, instantiates it, calls run(context), writes JSON to stdout
- Errors written to stderr with exit code 1
- Added tsup as devDep for building; tsx as runtime dep
- Added `bin.mrunner-plugin-runner` entry pointing to `./dist/runner.js`
- Added `scripts.build` to compile runner with tsup (ESM, node20 target)
- Files changed: packages/plugin/src/runner.ts, packages/plugin/package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - `tsx/esm/api` `register()` returns an `unregister` function — call it after the dynamic import
  - tsup build config can be entirely inline as CLI args, no tsup.config.ts needed
  - `biome check . --write` auto-reorganizes imports (merged two import statements into one)
  - `tsconfig.json` had `noEmit: true` — tsup bypasses this (uses esbuild, not tsc) so build still works
---

## 2026-03-09 - US-006
- Created `packages/plugin/schemas/plugin.schema.json` with full plugin manifest schema
- Created `packages/plugin/schemas/command.schema.json` with command config schema
- Both schemas: draft-07, required fields, additionalProperties: false, icon enum from ICON_MAP
- Icon enum sourced from `apps/renderer/src/lib/constants.ts` ICON_MAP keys (26 icons)
- mode enum: list, detail, action; runtime enum: node, deno, bun, python, bash
- Files changed: packages/plugin/schemas/plugin.schema.json, packages/plugin/schemas/command.schema.json
- **Learnings for future iterations:**
  - Icon values come from `ICON_MAP` in `apps/renderer/src/lib/constants.ts` — sync schemas with that if new icons are added
  - JSON schema files are not linted by biome (only JS/TS files checked), so pnpm check passes trivially for pure JSON additions
  - `additionalProperties: false` helps plugin authors catch typos in field names via IDE IntelliSense
---

## 2026-03-09 - US-007
- Added `run_plugin_command(plugin, command, context)` async function in `apps/launcher/src/plugins.rs`
- Added `find_command(plugins, command_id)` helper to look up plugin/command by ID
- Added `resolve_runner_path(plugin_dir)` to locate `{plugin_dir}/node_modules/@mrunner/plugin/dist/runner.js`
- Added `plugin_dir: PathBuf` field to `RegisteredPlugin` struct (serializes as `pluginDir` in JSON)
- Updated `load_scriptable_plugin` to populate `plugin_dir`
- Process spawning: `{runtime} {runner_path} {script_path}` with piped stdin/stdout/stderr
- Context JSON written to stdin; stdout/stderr read concurrently via `tokio::spawn`; 10-second timeout via `tokio::time::timeout`; process killed on timeout
- Non-zero exit code treated as error with stderr content included
- Added `tokio` dep with features: `process`, `time`, `io-util`, `rt`
- Files changed: apps/launcher/src/plugins.rs, apps/launcher/Cargo.toml, Cargo.lock
- **Learnings for future iterations:**
  - `tokio::join!` macro unavailable (tokio-macros 2.6.0 missing from local registry) — use `tokio::spawn` + sequential `.await` instead
  - `wait_with_output()` takes ownership of child; take stdout/stderr handles first then spawn tasks so child stays owned for kill()
  - `tokio::spawn` works in Tauri's multi-thread runtime with just the `rt` feature
  - `plugin_dir` is now a field on `RegisteredPlugin`, populated during discovery
---

## 2026-03-09 - US-008
- Added `PluginRegistry` type alias (`Mutex<Vec<plugins::RegisteredPlugin>>`) in lib.rs
- Updated `discover_plugins` Tauri command to accept `state: tauri::State<'_, PluginRegistry>`, cache results, and return `Result<Vec<...>, String>`
- Added `run_plugin_command` Tauri command: locks state in a scoped block to clone plugin+command, then calls `plugins::run_plugin_command` after the guard is dropped
- Registered `run_plugin_command` in invoke_handler
- Added `.manage(Mutex::new(Vec::<plugins::RegisteredPlugin>::new()))` to Builder
- Files changed: apps/launcher/src/lib.rs
- **Learnings for future iterations:**
  - `MutexGuard` is not `Send` — must drop it (via scoped block) before any `.await` in an async Tauri command
  - Pattern: `let (a, b) = { let guard = state.lock()?; let (x, y) = find(...)?; (x.clone(), y.clone()) };` — explicit block ensures guard drops before async work
  - `tauri::State<'_, Mutex<Vec<T>>>` needs the lifetime `'_` annotation
  - Two `.manage()` calls with the same type would conflict — each managed state type must be unique
---

## 2026-03-09 - US-009
- Added `ScriptableAction` interface and `CommandMode` type to `core/types/command.ts`
- Added `isScriptableAction` type guard
- Updated `core/types/index.ts` and `commands/types.ts` re-export barrels with new types/guards
- Updated `use-commands.ts` switch to include `scriptable` case (returns `{ success: true }` as placeholder for US-010)
- Rewrote `use-plugins.ts` to:
  - Load Tier 1 JSON plugins as before (with `readDir`/`readTextFile`)
  - Also call `invoke('discover_plugins')` for Tier 2 scriptable plugins
  - Convert each Tier 2 command to a `Command` with `ScriptableAction`, grouped by plugin name
  - Merge both tiers into a single command list
  - Both tiers' errors are handled independently (one failure doesn't block the other)
- Exported `ScriptableRegisteredPlugin`/`ScriptableRegisteredCommand` types from `use-plugins.ts` for downstream use
- Files changed: apps/renderer/src/core/types/command.ts, apps/renderer/src/core/types/index.ts, apps/renderer/src/commands/types.ts, apps/renderer/src/hooks/use-plugins.ts, apps/renderer/src/hooks/use-commands.ts
- **Learnings for future iterations:**
  - The `CommandAction` union has an exhaustive `never` check in `use-commands.ts`; adding a new action type REQUIRES adding a case to the switch or TypeScript will error
  - `core/types/index.ts` and `commands/types.ts` are re-export barrels — new exports must be threaded through both
  - Tier 1 directory-not-found is silently swallowed (try/catch inner block); Tier 2 errors are logged but don't abort loading
  - Scriptable commands use `icon: 'terminal'` as fallback since CommandIcon is a strict enum and plugin icons may not match
---

## 2026-03-09 - US-010
- Created `apps/renderer/src/components/command-palette/plugin-command-view.tsx` — sub-view shown when a list/detail-mode plugin command is active
- Updated `apps/renderer/src/App.tsx`:
  - Added `activeScriptableCommand` state (Command | null)
  - `onQueryReset` now also clears `activeScriptableCommand`
  - `handleSelect` intercepts scriptable actions: action mode → invoke('run_plugin_command') + hideWindow(); list/detail mode → setActiveScriptableCommand (sub-view)
  - Conditionally renders `PluginCommandView` or `CommandPalette` based on active command
- Added `plugins.back/running/success/error` i18n keys to en.json and pt-BR.json
- Exported `PluginCommandView` from command-palette index.ts
- Files changed: apps/renderer/src/App.tsx, apps/renderer/src/components/command-palette/plugin-command-view.tsx, apps/renderer/src/components/command-palette/index.ts, apps/renderer/src/locales/en.json, apps/renderer/src/locales/pt-BR.json
- **Learnings for future iterations:**
  - Escape key must use `{ capture: true }` in the plugin sub-view so it intercepts before the @mrunner/ui Command component handles it
  - `i18n.language` is accessible via `const { i18n } = useTranslation()` — needed for passing locale in CommandContext
  - `homeDir()` from `@tauri-apps/api/path` is async — must await it inside handleSelect
  - The `isScriptableAction` type guard is exported from `@/commands/types` (re-exported barrel)
  - `PluginCommandView` uses an empty `<div>` loading placeholder in `CommandList` rather than `CommandEmpty` (which only shows when there are no matching items, not when waiting for results)
---

## 2026-03-09 - US-011
- Updated `PluginCommandView` to execute list-mode plugin commands with debouncing and display results
- Added `@mrunner/plugin` as workspace dep in renderer's package.json (types pointed to src/index.ts)
- `runCommand` uses `invoke('run_plugin_command')` with current query as CommandContext
- Debounced effect (300ms) re-executes command on query change; runs immediately on mount
- Loading indicator shown inline in header; error state shown in list area
- `ListItem[]` rendered using `CommandItem` from @mrunner/ui with icon, title, subtitle, accessories
- Item selection triggers `executeAction(item.actions[0])` — handles url/open/copy/shell/notification/push
- `shouldFilter={false}` on Command component since filtering is handled server-side by the plugin
- Files changed: apps/renderer/src/components/command-palette/plugin-command-view.tsx, apps/renderer/package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - `@mrunner/plugin` package exports directly from `src/index.ts` — no build needed for type imports in renderer
  - `item.actions[0]` can be `undefined` even when `actions.length > 0` in TypeScript strict mode — use explicit null check
  - `shouldFilter={false}` is needed on the cmdk Command component when items are managed externally (no client-side filtering)
  - Use `search.empty` key for "no results" message (not `search.noResults`)
---

## 2026-03-09 - US-012
- Added `react-markdown` dependency to apps/renderer for markdown rendering
- Updated `plugin-command-view.tsx`:
  - Added `DetailResult` import from `@mrunner/plugin` and `isDetailResult` type guard
  - Added `sendNotification` from `@tauri-apps/plugin-notification` for notification actions
  - Added `ReactMarkdown` import for rendering detail mode content
  - Added `detailResult` state alongside existing `items`/`loading`/`error` states
  - `runCommand` now dispatches to `setDetailResult` or `setItems` based on result type
  - Detail mode renders markdown via `<ReactMarkdown>` with action buttons below
  - List mode: 300ms debounce; detail mode: runs immediately (0ms delay)
  - `notification` action case now calls `sendNotification` (previously just `console.info`)
  - Search input hidden for detail mode (no query-driven filtering)
- Updated `App.tsx`:
  - Added `sendNotification` import from `@tauri-apps/plugin-notification`
  - Added `t` to `useTranslation()` destructuring
  - Action mode now shows OS success notification (`plugins.success`) on success
  - Action mode now shows OS error notification (`plugins.error`) on failure
  - Added `t` to `handleSelect` useCallback dependency array
- Files changed: apps/renderer/src/components/command-palette/plugin-command-view.tsx, apps/renderer/src/App.tsx, apps/renderer/package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - `react-markdown` v10 is ESM-only; works fine with Vite (no extra config needed)
  - `DetailResult.actions` items each optionally have a `title` field — use it for button labels with fallback to `action.type`
  - Detail mode should NOT show `CommandInput` (no query-based filtering makes sense for static markdown)
  - The existing `plugins.success` / `plugins.error` / `plugins.running` i18n keys were sufficient — no new keys needed
---

## 2026-03-09 - US-013
- Created shared utility `apps/renderer/src/lib/execute-plugin-action.ts` with `executePluginAction(action, options?)` function
- Handles all action types: url (open in browser), open (open file/folder), copy (clipboard), shell (run_shell_command IPC), notification (OS notification), push (navigate to another plugin command)
- push action delegates to optional `onPush` callback to keep the utility free of React state
- Removed local `executeAction` function from `plugin-command-view.tsx`; replaced all callsites with shared `executePluginAction`
- Added `onPushCommand?: (commandId: string) => void` prop to `PluginCommandView`
- Wired `handleItemSelect` and detail mode action buttons through `onPushCommand`
- Added `handlePushCommand` in `App.tsx`: looks up target command by `ScriptableAction.commandId` in `allItems`, sets it as `activeScriptableCommand`
- Files changed: apps/renderer/src/lib/execute-plugin-action.ts (new), apps/renderer/src/components/command-palette/plugin-command-view.tsx, apps/renderer/src/App.tsx
- **Learnings for future iterations:**
  - Shared action utility pattern: pass navigation callbacks as options to keep utility stateless and reusable
  - `push` action's `action.command` field is the plugin commandId (format: `plugin-id:command-name`) — match against `ScriptableAction.commandId` in allItems
  - biome auto-fixed import ordering in plugin-command-view.tsx (1 file fixed on `pnpm check --write`)
  - executePluginAction is now the canonical place for all plugin action dispatch — add new action types there first
---

## 2026-03-09 - US-014
- Created `apps/renderer/src/components/settings/plugins-tab.tsx`
- Lists all Tier 2 scriptable plugins discovered via `invoke('discover_plugins')`
- Enable/disable toggle per plugin; disabled plugin IDs persisted to `preferences.json` under `plugins.disabledPlugins` key
- Expandable section per plugin showing its commands (title, description, mode)
- "Open Plugins Folder" button opens `~/.config/mrunner/plugins/` via Tauri shell `open()`
- Added optional `plugins` field to `UserPreferences` interface and `UserPreferencesSchema` in `core/types/command.ts`
- Updated `settings-sheet.tsx`: added "Plugins" to TABS array, Alt+3 navigation, tab label, and `<PluginsTab />` rendering
- Added `settings.tabPlugins` and `settings.plugins.*` i18n keys to en.json and pt-BR.json
- Files changed: apps/renderer/src/components/settings/plugins-tab.tsx (new), apps/renderer/src/components/settings/settings-sheet.tsx, apps/renderer/src/core/types/command.ts, apps/renderer/src/locales/en.json, apps/renderer/src/locales/pt-BR.json, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - `UserPreferences` interface and `UserPreferencesSchema` in `core/types/command.ts` must stay in sync — updating one requires updating the other
  - Zod's `z.object()` strips unknown keys on parse; adding new preference keys requires updating the schema or they'll be dropped on next write
  - For preferences that don't exist yet, read the raw JSON and spread-merge (don't rely solely on schema-parsed value) to avoid overwriting unrelated keys
  - Settings tabs: add to TABS const, update `Alt+N` range check, add label in tab button render, add content block in SheetBody, add i18n key
---
